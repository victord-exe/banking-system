# ==============================================================================
# HLABS Banking System - Docker Environment Configuration
# ==============================================================================
# This file contains default values for running the entire system with Docker.
# The evaluator can run `docker-compose up` without any configuration.
#
# For production: Create a .env file and override these values with real secrets.
# ==============================================================================

# ------------------------------------------------------------------------------
# PostgreSQL Database (User Authentication)
# ------------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=banking_user
POSTGRES_PASSWORD=banking_secure_password_2024
POSTGRES_DB=banking_system
POSTGRES_DSN=postgresql://banking_user:banking_secure_password_2024@postgres:5432/banking_system?sslmode=disable

# ------------------------------------------------------------------------------
# TigerBeetle (Financial Transactions Database)
# ------------------------------------------------------------------------------
TIGERBEETLE_HOST=tigerbeetle
TIGERBEETLE_PORT=3000
TIGERBEETLE_CLUSTER_ID=0
TIGERBEETLE_REPLICA_ID=0

# System bank account ID in TigerBeetle (for deposits/withdrawals)
SYSTEM_BANK_ACCOUNT_ID=1

# ------------------------------------------------------------------------------
# Backend Server Configuration
# ------------------------------------------------------------------------------
SERVER_PORT=8080
GIN_MODE=release
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# JWT Authentication
# ------------------------------------------------------------------------------
# NOTE: This is a development secret. CHANGE THIS IN PRODUCTION!
JWT_SECRET=hlabs-banking-system-jwt-secret-key-change-in-production-2024
JWT_EXPIRATION_HOURS=24

# ------------------------------------------------------------------------------
# OpenRouter / MCP (AI Chat Integration)
# ------------------------------------------------------------------------------
# NOTE: You need a real API key for AI chat to work.
# Get one at: https://openrouter.ai
# If not provided, the chat feature will show an error but the rest will work.
OPENROUTER_API_KEY=sk-or-v1-YOUR-API-KEY-HERE
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ------------------------------------------------------------------------------
# Frontend Configuration
# ------------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:8080/api

# ------------------------------------------------------------------------------
# CORS Configuration
# ------------------------------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://frontend:5173

# ------------------------------------------------------------------------------
# Docker Network
# ------------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=hlabs-banking

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
# Enable data seeding on first run (loads test users)
ENABLE_DATA_SEEDING=true

# Seed users from this file
SEED_DATA_FILE=/app/datos-prueba-HNL.json
